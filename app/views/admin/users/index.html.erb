<div class='row'>
  <div class='col-lg-12'>

    <div class='row'>
      <div class='col-lg-12'>
        <div class='top-actions pull-right'>
          <%= link_to 'Add new user', new_admin_user_path, class: '' %>
        </div>
      </div>
    </div>

    <table class="table table-striped">
      <tr>
        <th>Name</th>
        <th>Email</th>
        <th>Actions</th>
      </tr>
      <% @users.each do |u| %>
        <tr>
          <td> <%= u.name %> </td>
          <td> <%= u.email %> </td>
          <td> <%= link_to 'Edit', edit_admin_user_path(u) %> </td>
        </tr>
      <% end %>
    </table>
  </div>
</div>
